SPOILER


































































































*
return a+b:

**
return -(-a-b);

***
return new Array(a).concat(new Array(b)).length;